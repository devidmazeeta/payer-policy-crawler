[pytest]
# The suite is fully offline: no test makes a network request.
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning:crawler.*
addopts = -q --strict-markers
